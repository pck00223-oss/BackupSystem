# 数据备份软件设计与实现方案
> 项目定位：面向计算机科学与技术、软件工程方向的复试项目。  
> 核心重点：**软件工程、面向对象设计、Windows 文件 I/O、增量备份、任务调度、异常处理、测试与工程化**。  
> 设计原则：控制开发规模，优先保证核心链路稳定，再增加压缩、加密等扩展能力。

---

# 1. 项目概述
## 1.1 项目名称
**基于 C++ / Windows 的数据备份软件**

## 1.2 项目定位
本项目实现一个运行于 Windows 操作系统的本地数据备份工具。
软件能够扫描用户指定的目录树，将文件及必要的文件信息保存到指定的备份位置，并支持从备份数据恢复原始目录结构和文件内容。

项目重点放在：
- 文件系统操作
- 全量备份
- 增量备份
- 文件变更检测
- 备份任务调度
- 异常处理
- 日志记录
- 备份数据管理
- 数据完整性验证
- 面向对象设计
- 模块化架构
- 软件测试

压缩、加密和 GUI 作为辅助功能实现，不将项目重点转向密码学、网络安全或分布式系统。

---

# 2. 项目目标
## 2.1 核心目标
实现一套完整的本地备份流程：
```text
选择源目录
    ↓
扫描目录
    ↓
获取文件信息
    ↓
生成文件清单 Manifest
    ↓
判断文件是否发生变化
    ↓
选择需要备份的文件
    ↓
写入备份数据
    ↓
保存备份清单
    ↓
记录任务结果

选择备份文件
    ↓
读取 Manifest
    ↓
解析备份数据
    ↓
创建目录结构
    ↓
恢复文件
    ↓
恢复支持的元数据
    ↓
Hash 校验
    ↓
生成恢复结果
```

## 2.2 项目最终达到的效果
至少完成：
- 全量备份
- 增量备份
- 数据恢复
- 自定义文件筛选
- 备份历史查询
- 手动执行备份
- 简单定时备份
- 文件 Hash 校验
- 日志记录
- 异常处理
- GUI
- 测试

在核心功能稳定后增加：
- 备份包打包与解包
- Zstandard 等压缩方式
- AES 基础加密
- 备份完整性验证

---

# 3. 需求分析
## 3.1 功能需求
### 3.1.1 数据备份
用户指定源目录和备份目标位置后，软件递归扫描源目录。
系统获取文件的基本信息，并根据备份模式决定需要保存哪些文件。

支持：
- 全量备份
- 增量备份

**全量备份**
第一次备份时，将符合条件的文件全部保存。
```text
源目录
    ↓
扫描
    ↓
筛选
    ↓
全部备份
```

**增量备份**
后续备份时，将当前目录状态与上一次备份的 Manifest 进行比较，只处理发生变化或新增的文件。
```text
当前目录
    ↓
扫描
    ↓
读取旧 Manifest
    ↓
文件比较
    ↓
识别新增 / 修改 / 删除
    ↓
只备份需要更新的数据
```

## 3.2 数据还原
用户选择已有备份数据和目标目录。
系统读取备份数据中的 Manifest，根据文件原始相对路径创建目录，并恢复文件。
```text
备份数据
    ↓
解析 Manifest
    ↓
创建目录
    ↓
恢复文件
    ↓
恢复元数据
    ↓
Hash 校验
```

## 3.3 自定义备份
用户可以设置筛选规则。
支持的筛选条件：
- 文件路径
- 文件扩展名
- 文件大小
- 修改时间
- 用户相关信息（在 Windows 文件系统和权限允许的情况下）

例如：

## 3.3 自定义备份
用户可以设置筛选规则。
支持的筛选条件：
- 文件路径
- 文件扩展名
- 文件大小
- 修改时间
- 用户相关信息（在 Windows 文件系统和权限允许的情况下）

例如：
```
路径：D:\Project
类型：.cpp / .h
大小：> 1 KB
修改时间：最近 30 天
```

筛选逻辑由 C++ 实现。
GUI 只负责收集条件。

## 3.4 文件类型支持
系统主要处理 Windows 普通文件和目录。
对于符号链接等特殊文件系统对象，根据 Windows API 能力、文件系统类型和当前权限进行识别和处理。
特殊对象的支持范围需要以实际实现为准。

对于不能安全恢复的对象：
```text
识别
    ↓
记录
    ↓
跳过或按照策略处理
    ↓
向用户报告
```

禁止静默忽略错误。

## 3.5 文件元数据
根据 Windows API 能力保存必要的文件信息。
建议支持：
- 相对路径
- 文件大小
- 文件类型
- 创建时间
- 修改时间
- 访问时间
- 文件属性
- 可访问情况下的权限或所有者信息

项目第一阶段优先完成：
- 路径
- 大小
- 修改时间
- 文件类型

高级元数据作为后续扩展。

## 3.6 打包与解包
将多个文件组织为一个备份包。
推荐自定义简单备份格式：
```text
Backup File
│
├── Header
│   ├── Version
│   ├── Backup ID
│   ├── File Count
├── Manifest
├── File Data
└── Integrity Information
```

备份包中保存恢复所需的文件信息。
解包时读取 Header 和 Manifest，根据记录恢复文件。

## 3.7 压缩与解压
压缩功能用于降低备份文件的存储空间。
建议采用接口抽象：
```cpp
class ICompressor {
public:
    virtual ~ICompressor() = default;

    virtual bool compress(
        const std::vector<std::byte>& input,
        std::vector<std::byte>& output) = 0;

    virtual bool decompress(
        const std::vector<std::byte>& input,
        std::vector<std::byte>& output) = 0;
};
```

具体实现可以使用成熟压缩库。
推荐项目第一版只接入一种压缩算法，例如 Zstandard。

架构上保留扩展空间：
```text
ICompressor
    │
    └── ZstdCompressor
```

如果课程要求相关算法自行实现，则必须按照课程要求调整。

## 3.8 加密与解密
提供基础的备份包加密功能。
流程：
```text
文件
    ↓
打包
    ↓
压缩
    ↓
AES 加密
    ↓
备份文件
```

恢复：
```text
备份文件
    ↓
AES 解密
    ↓
解压
    ↓
解包
    ↓
恢复文件
```

项目不以密码学研究为目标。
第一版只要求：
- 密码输入
- AES 加密
- AES 解密
- 错误密码检测
- 不在普通日志中记录明文密码

如果使用 Windows CNG 等系统密码学能力，应封装在独立模块中。

## 3.9 任务调度
支持：
- 立即执行
- 简单定时执行

例如：每天 20:00

调度器负责判断任务是否达到执行条件。
```text
TaskScheduler
    ↓
检查任务
    ↓
是否到执行时间
    ↓
BackupManager
```

不实现复杂的线程池、分布式调度或任务依赖系统。

## 3.10 备份历史
记录：
- 备份时间
- 源路径
- 目标路径
- 备份类型
- 文件数量
- 成功数量
- 失败数量
- 数据大小
- 是否成功
- 错误信息

例如：
```
2026-09-04 20:00
类型：增量备份
文件：135
新增：3
修改：7
失败：0
状态：成功
```

## 3.11 图形界面
GUI 只负责：
- 用户输入
- 参数配置
- 任务启动
- 任务取消
- 进度显示
- 日志显示
- 历史记录显示
- 恢复操作

GUI 不负责：
- 文件扫描
- 文件比较
- Hash 计算
- 文件复制
- 打包
- 压缩
- 加密
- 业务判断

推荐结构：
```text
GUI
    ↓
Application / Service
    ↓
Core Business
    ↓
File Engine
    ↓
Windows
```

---

# 4. 非功能需求
## 4.1 正确性
备份后恢复的数据应与源文件一致。
至少验证：
- 文件数量
- 相对路径
- 文件大小
- 文件内容
- Hash
- 支持的元数据

## 4.2 健壮性
程序应处理：
- 路径不存在
- 权限不足
- 文件被其他程序占用
- 文件在备份过程中消失
- 目标文件已经存在
- 磁盘空间不足
- 备份文件损坏
- Manifest 损坏
- 密码错误
- 用户取消任务

## 4.3 可维护性
代码采用面向对象设计。
避免：
- 超长函数
- 超大类
- 重复代码
- 全局变量滥用
- GUI 与业务逻辑高度耦合
- 大量嵌套条件
- 手工资源管理失控

## 4.4 可扩展性
压缩、加密等功能通过接口隔离。
例如：
```text
ICompressor
    ├── NoCompressor
    └── ZstdCompressor

IEncryptor
    ├── NoEncryptor
    └── AesEncryptor
```

新增算法时尽量不修改 BackupManager 的核心流程。

---

# 5. 系统总体架构
采用分层架构。
```text
┌─────────────────────────────┐
│           GUI 层            │
│ 参数配置 / 进度 / 历史 / 日志 │
└──────────────┬──────────────┘
               ↓
┌─────────────────────────────┐
│       Application 层        │
│ 任务管理 / 流程控制 / 调度   │
└──────────────┬──────────────┘
               ↓
┌─────────────────────────────┐
│        Business 层          │
│ Backup / Restore / Filter   │
│ Manifest / Archive / Hash   │
└──────────────┬──────────────┘
               ↓
┌─────────────────────────────┐
│       File Engine 层        │
│ Scan / Compare / Copy       │
└──────────────┬──────────────┘
               ↓
┌─────────────────────────────┐
│      Windows File System    │
│       Win32 / Windows API   │
└─────────────────────────────┘
```

---

# 6. 核心类设计
## 6.1 FileInfo
用于表示一个文件的基本信息。
```cpp
struct FileInfo {
    std::wstring relativePath;
    uint64_t size;
    uint64_t modifiedTime;
    FileType type;
    std::string hash;
};
```

职责：
- 保存文件基本属性
- 作为扫描、比较、Manifest 的数据对象

## 6.2 FileScanner
职责：
- 递归遍历目录
- 获取文件信息
- 生成 FileInfo

不负责：
- 备份
- 压缩
- 加密
- GUI

## 6.3 FileComparator
职责：
- 比较当前文件状态和历史 Manifest
- 判断新增、修改、删除、未变化文件

核心策略：
```text
路径
    ↓
是否存在历史记录？
    ├── 否 → 新文件
    └── 是
         ↓
       比较大小
         ↓
       比较修改时间
         ↓
       必要时计算 Hash
         ↓
       判断是否变化
```

## 6.4 Manifest
记录一次备份对应的文件状态。
建议保存：
- 相对路径
- 文件大小
- 修改时间
- 文件类型
- Hash
- 备份数据位置

Manifest 是：
- 增量检测
- 恢复
- 完整性校验

共同依赖的数据基础。

## 6.5 BackupManager
负责组织备份流程。
```text
BackupManager
    ↓
FileScanner
    ↓
FileComparator
    ↓
BackupTask
    ↓
ArchiveWriter
    ↓
Compressor
    ↓
Encryptor
```

BackupManager 不应该直接承担底层文件读取的全部工作。

## 6.6 RestoreManager
负责恢复流程：
```text
RestoreManager
    ↓
Decryptor
    ↓
Decompressor
    ↓
ArchiveReader
    ↓
Manifest
    ↓
FileSystem
```

## 6.7 FileSystem
负责封装 Windows 文件系统操作。
例如：
```cpp
class FileSystem {
public:
    bool exists(const std::wstring& path);
    bool createDirectory(const std::wstring& path);
    bool readFile(...);
    bool writeFile(...);
    bool copyFile(...);
    bool getFileInfo(...);
};
```

具体实现使用 Windows API 或符合课程要求的 C++ 文件系统能力。
如果课程明确要求贴近 Windows 操作系统，应优先使用 Win32 API，并将其集中封装。

## 6.8 ArchiveWriter
负责：
- 创建备份包
- 写 Header
- 写 Manifest
- 写文件数据
- 写校验信息

## 6.9 ArchiveReader
负责：
- 读取 Header
- 读取 Manifest
- 定位文件数据
- 检查备份格式
- 提供恢复所需数据

## 6.10 TaskScheduler
负责：
- 管理备份任务
- 检查任务时间
- 启动任务
- 取消任务

## 6.11 Logger
统一记录：
- INFO
- WARNING
- ERROR

日志应包含：
- 时间
- 级别
- 模块
- 消息

敏感信息，例如密码，不得写入普通日志。

---

# 7. 三个核心业务流程
## 7.1 流程一：数据备份
**业务描述**
将目录树中的文件数据保存到指定位置。

**流程**
```text
用户选择源目录
    ↓
读取 BackupConfig
    ↓
FileScanner 扫描目录
    ↓
生成 FileInfo
    ↓
FileFilter 筛选
    ↓
全量 / 增量判断
    ↓
FileComparator
    ↓
得到待备份文件
    ↓
ArchiveWriter 写入备份包
    ↓
压缩（可选）
    ↓
加密（可选）
    ↓
保存 Manifest
    ↓
记录日志
    ↓
生成 BackupResult
```

**全量备份**
```text
旧 Manifest 不存在
    ↓
所有符合条件文件进入备份集合
```

**增量备份**
```text
读取旧 Manifest
    ↓
扫描当前目录
    ↓
路径匹配
    ↓
大小 / 修改时间快速比较
    ↓
必要时 Hash 比较
    ↓
识别新增 / 修改文件
    ↓
只处理变化文件
```

## 7.2 流程二：数据还原
**业务描述**
将备份数据恢复到指定目录。

**流程**
```text
选择备份文件
    ↓
读取 Header
    ↓
检查版本和格式
    ↓
解密（如果启用）
    ↓
解压（如果启用）
    ↓
ArchiveReader
    ↓
读取 Manifest
    ↓
创建目录
    ↓
恢复文件数据
    ↓
恢复支持的元数据
    ↓
Hash 校验
    ↓
生成 RestoreResult
```

## 7.3 流程三：自定义备份
**业务描述**
根据用户指定的规则筛选需要备份的文件。

**流程**
```text
用户选择源目录
    ↓
读取筛选条件
    ↓
FileScanner
    ↓
逐个处理 FileInfo
    ↓
路径判断
    ↓
类型判断
    ↓
时间判断
    ↓
大小判断
    ↓
用户条件判断
    ↓
得到符合条件的文件集合
    ↓
交给 BackupManager
```

自定义备份不重复实现备份过程。
它只负责：
```text
筛选
    ↓
生成文件集合
    ↓
交给统一备份流程
```

---

# 8. 增量备份设计
增量备份是本项目的核心技术点。

## 8.1 为什么需要 Manifest
如果每次备份都重新读取全部文件并计算 Hash：
```text
扫描
    ↓
读取所有文件
    ↓
Hash 全部文件
    ↓
比较
```

数据量较大时会产生大量不必要的磁盘读取。
因此保存上一次备份的 Manifest。

## 8.2 两级变更检测
采用：
- 第一阶段：元数据快速判断
- 第二阶段：Hash 二次确认

流程：
```text
当前文件
    ↓
路径是否存在历史记录？
    ├── 否 → 新文件
    └── 是
         ↓
       文件大小是否变化？
       ├── 是 → 文件变化
       └── 否
            ↓
          修改时间是否变化？
          ├── 否 → 暂认为未变化
          └── 是
               ↓
             计算 Hash
               ↓
          Hash 是否一致？
          ├── 是 → 未变化
          └── 否 → 已变化
```

这种方法减少不必要的文件内容读取，同时保留 Hash 作为可靠的变化确认手段。

## 8.3 文件状态
建议定义：
```cpp
enum class FileChangeType {
    Unchanged,
    Added,
    Modified,
    Deleted
};
```

## 8.4 删除文件
增量备份中需要考虑源目录删除文件的情况。
例如：
```
第一次： A.txt B.txt C.txt
第二次： A.txt C.txt
```

系统可以记录：
```
B.txt → Deleted
```

是否在恢复时删除目标目录中的 B.txt，需要通过明确的恢复策略决定。
建议第一版采用：
- 默认不自动删除目标文件
- 避免恢复操作造成意外数据丢失

如果后续实现镜像恢复，再增加明确的：
- 镜像恢复模式

---

# 9. 备份文件格式
建议定义简单、自有、可解释的备份格式。
例如：
```text
┌─────────────────────────┐
│ Magic                   │
├─────────────────────────┤
│ Version                 │
├─────────────────────────┤
│ Flags                   │
├─────────────────────────┤
│ Backup ID               │
├─────────────────────────┤
│ File Count              │
├─────────────────────────┤
│ Manifest Size           │
├─────────────────────────┤
│ Manifest                │
├─────────────────────────┤
│ File Data               │
├─────────────────────────┤
│ Integrity Information   │
└─────────────────────────┘
```

其中：
- **Magic**：用于识别文件是否属于本软件。
- **Version**：用于支持以后修改格式。
- **Flags**：记录是否压缩、是否加密。

---

# 10. 文件系统设计
## 10.1 Windows 文件操作
文件系统操作应尽量集中在 FileSystem 模块。
推荐：
```text
业务层
    ↓
FileSystem
    ↓
Windows API
```

Windows API 可以包括：
- FindFirstFileW
- FindNextFileW
- CreateFileW
- ReadFile
- WriteFile
- CloseHandle
- CreateDirectoryW
- GetFileAttributesW
- GetFileInformationByHandle

具体 API 根据实际实现选择。

## 10.2 资源管理
Windows HANDLE 等资源必须可靠释放。
推荐采用 RAII：
```cpp
class FileHandle {
public:
    FileHandle();
    ~FileHandle();

    FileHandle(const FileHandle&) = delete;
    FileHandle& operator=(const FileHandle&) = delete;
};
```

避免：
```cpp
HANDLE handle = CreateFileW(...);
// 中间出现异常
CloseHandle(handle);
```

导致异常路径下资源泄漏。

---

# 11. 排序设计
如果业务要求对文件进行排序：
- 按路径
- 按文件名
- 按大小
- 按修改时间

排序规则由 C++ 实现。

如果课程要求自行实现排序算法，则不得使用：
- std::sort

等现成排序算法。
可以自行实现：
- 插入排序
- 选择排序
- 归并排序
- 快速排序

具体选择根据文件规模和课程要求决定。
项目中不要为了体现算法而加入复杂排序。

---

# 12. 配置设计
配置至少包括：
- 源路径
- 备份路径
- 备份类型
- 筛选条件
- 执行时间
- 压缩方式
- 加密方式

示例：
```
source=C:\Data
target=D:\Backup
type=incremental
schedule=20:00
compression=zstd
encryption=aes
```

如果允许使用第三方 JSON 库，也可以采用 JSON 保存配置。
如果课程要求核心部分尽量减少第三方依赖，则优先使用简单自定义配置格式。

---

# 13. 项目目录结构
推荐：
```text
BackupSystem/
│
├── CMakeLists.txt
├── README.md
│
├── include/
│   ├── BackupManager.h
│   ├── RestoreManager.h
│   ├── FileScanner.h
│   ├── FileComparator.h
│   ├── FileSystem.h
│   ├── FileInfo.h
│   ├── Manifest.h
│   ├── ArchiveWriter.h
│   ├── ArchiveReader.h
│   ├── TaskScheduler.h
│   ├── Logger.h
│   ├── Compressor.h
│   └── Encryptor.h
│
├── src/
│   ├── BackupManager.cpp
│   ├── RestoreManager.cpp
│   ├── FileScanner.cpp
│   ├── FileComparator.cpp
│   ├── FileSystem.cpp
│   ├── Manifest.cpp
│   ├── ArchiveWriter.cpp
│   ├── ArchiveReader.cpp
│   ├── TaskScheduler.cpp
│   ├── Logger.cpp
│   ├── Compressor.cpp
│   └── Encryptor.cpp
│
├── gui/
│   └── ...
│
├── tests/
│   ├── FileScannerTest.cpp
│   ├── FileComparatorTest.cpp
│   ├── ManifestTest.cpp
│   └── BackupTest.cpp
│
├── config/
│   └── default.conf
│
└── docs/
    ├── requirements.md
    ├── design.md
    ├── testing.md
    └── use_case.md
```

实际开发时可以根据代码量合并文件，避免为了形式制造大量空壳类。

---

# 14. 代码实现原则
## 14.1 核心业务语言
核心业务逻辑全部使用 C++。
禁止使用脚本语言实现：
- 文件扫描
- 文件比较
- 文件排序
- 文件复制
- 备份
- 恢复
- 打包
- 解包
- 压缩业务流程
- 加密业务流程
- 任务调度

GUI 可以根据项目允许情况使用相应工具，但 GUI 不得承载业务逻辑。

## 14.2 面向对象原则
**单一职责**
一个类应承担清晰职责。
例如：
- FileScanner → 扫描
- FileComparator → 比较
- BackupManager → 组织备份
- RestoreManager → 组织恢复
- Logger → 日志

## 14.3 避免巨型类
禁止出现：
```cpp
class BackupManager {
    // 扫描
    // 筛选
    // Hash
    // 复制
    // 压缩
    // 加密
    // 日志
    // GUI
    // 配置
    // 恢复
};
```

BackupManager 只负责组织备份流程。

## 14.4 避免 GUI 屎山
禁止：
```cpp
void MainWindow::onBackupClicked() {
    // 几百行文件处理逻辑
}
```

应该：
```text
GUI
    ↓
创建 BackupTask
    ↓
BackupManager
    ↓
FileScanner
    ↓
FileComparator
    ↓
ArchiveWriter
```

GUI 负责展示。
业务模块负责业务。
文件模块负责文件操作。

## 14.5 RAII
优先使用：
- std::unique_ptr
- std::shared_ptr
- std::vector
- std::string
- std::wstring

Windows HANDLE 等操作系统资源使用 RAII 包装。
尽量减少裸 new/delete。

## 14.6 错误处理
所有重要文件操作都必须检查结果。
错误类型至少包括：
- FileNotFound
- AccessDenied
- FileLocked
- DiskFull
- InvalidPath
- InvalidBackup
- HashMismatch
- WrongPassword
- Cancelled
- UnknownError

---

# 15. 日志系统
日志格式：
```text
[2026-09-04 20:00:01] [INFO] Backup started
[2026-09-04 20:00:03] [INFO] Scanned 100 files
[2026-09-04 20:00:04] [INFO] 8 files changed
[2026-09-04 20:00:10] [ERROR] Cannot read xxx.txt
[2026-09-04 20:00:11] [INFO] Backup finished
```

日志等级：
- INFO
- WARNING
- ERROR

不得记录：
- 用户密码

---

# 16. 任务取消
备份任务支持取消。
例如：
```cpp
class BackupTask {
public:
    void cancel();
    bool isCancelled() const;
};
```

核心循环：
```cpp
for (...) {
    if (task.isCancelled())
    {
        break;
    }

    backupFile(...);
}
```

取消后：
- 停止新增文件处理
- 关闭当前资源
- 写入日志
- 生成任务结果

不要求实现复杂的事务回滚。

---

# 17. 测试设计
## 17.1 测试目标
测试重点不是只验证"正常情况下能不能复制文件"，还要验证：
- 边界条件
- 异常条件
- 数据一致性
- 增量检测正确性
- 任务取消
- 文件损坏
- 权限错误

## 17.2 单元测试
重点测试：
- FileComparator
- Manifest
- FileFilter
- Hash
- 配置解析
- 路径处理

例如：
```
输入：
旧文件 size=100
新文件 size=100
时间相同

预期：
Unchanged
```

## 17.3 全量备份测试
| 编号 | 场景 | 预期 |
|------|------|------|
| B01 | 单文件 | 成功 |
| B02 | 多文件 | 全部备份 |
| B03 | 多级目录 | 目录结构正确 |
| B04 | 空目录 | 正确处理 |
| B05 | 大文件 | 内容一致 |
| B06 | 不存在源目录 | 返回错误 |
| B07 | 无权限目录 | 记录错误 |

## 17.4 增量备份测试
| 编号 | 场景 | 预期 |
|------|------|------|
| I01 | 第一次备份 | 全部文件备份 |
| I02 | 无文件变化 | 不重复备份 |
| I03 | 新增文件 | 只备份新增文件 |
| I04 | 修改文件 | 只备份修改文件 |
| I05 | 删除文件 | 正确记录删除状态 |
| I06 | 文件大小变化 | 检测为变化 |
| I07 | 时间变化 | 进入进一步判断 |
| I08 | Hash 变化 | 检测为变化 |
| I09 | 多种变化混合 | 分类正确 |

## 17.5 恢复测试
| 编号 | 场景 | 预期 |
|------|------|------|
| R01 | 单文件恢复 | 内容一致 |
| R02 | 多级目录恢复 | 目录结构一致 |
| R03 | 多文件恢复 | 文件全部恢复 |
| R04 | 目标目录不存在 | 按设计创建 |
| R05 | 目标文件已存在 | 按冲突策略处理 |
| R06 | Hash 不一致 | 报告校验失败 |
| R07 | 备份文件损坏 | 拒绝或停止恢复 |

## 17.6 异常测试
重点场景：
- 源路径不存在
- 目标路径不存在
- 目标路径无权限
- 源文件被删除
- 源文件无法读取
- 目标磁盘空间不足
- 备份文件损坏
- Manifest 损坏
- 用户输入错误密码
- 备份过程中取消

## 17.7 数据一致性测试
备份：
```text
原始文件
    ↓
备份
```

恢复：
```text
备份
    ↓
恢复文件
```

比较：
- 路径
- 大小
- 内容
- Hash
- 元数据

只有验证通过后，才认为恢复成功。

---

# 18. 测试数据设计
建立专门测试目录：
```text
TestData/
│
├── normal/
│   ├── a.txt
│   ├── b.cpp
│   └── c.jpg
│
├── nested/
│   ├── level1/
│   │   ├── a.txt
│   │   └── level2/
│   │       └── b.txt
│
├── empty/
│
└── large/
    └── large.bin
```

测试时人为制造：
- 新增
- 修改
- 删除
- 重命名
- 大文件
- 空文件
- 特殊字符路径
- 长路径
- 权限异常

---

# 19. 项目开发阶段
**Phase 1：核心文件引擎**
目标：
- FileSystem
- FileInfo
- FileScanner
- FileCopier

完成：
- 扫描
- 复制
- 恢复

**Phase 2：Manifest**
实现：
- Manifest
- FileComparator

完成：
- 第一次全量备份
- 第二次增量备份

这是项目最重要的阶段。

**Phase 3：异常和日志**
实现：
- Logger
- Error
- BackupResult
- RestoreResult

处理：
- 权限
- 文件消失
- 路径错误
- 磁盘错误
- 取消

**Phase 4：任务调度**
实现：
- BackupTask
- TaskScheduler

支持：
- 立即执行
- 每天指定时间执行

**Phase 5：备份归档**
实现：
- ArchiveWriter
- ArchiveReader

形成单个备份文件。

**Phase 6：Hash 校验**
增加：
- HashCalculator

用于：
- 增量判断
- 恢复校验
- 备份完整性检查

**Phase 7：压缩**
增加：
- ICompressor
- ZstdCompressor

如果核心功能尚未稳定，不进入该阶段。

**Phase 8：AES**
增加：
- IEncryptor
- AesEncryptor

只实现基础备份包加密与解密。

**Phase 9：GUI**
最后接入：
- 任务列表
- 新建任务
- 立即备份
- 定时备份
- 恢复
- 进度
- 日志
- 历史

GUI 不重新实现业务逻辑。

---

# 20. 开发优先级
**P0：必须完成**
- 全量备份
- 恢复
- Windows 文件操作
- Manifest
- 增量备份
- 异常处理
- 日志
- 测试

**P1：建议完成**
- 自定义筛选
- 任务调度
- 备份历史
- Hash 完整性验证
- GUI

**P2：有余力再完成**
- 打包
- 压缩
- AES
- 特殊文件支持
- 高级元数据

---

# 21. 两档最终版本
## 21.1 稳妥版
完成：
- Windows 文件扫描
- 全量备份
- 增量备份
- Manifest
- Hash
- 恢复
- 任务调度
- 异常处理
- 日志
- 自定义筛选
- GUI
- 测试

特点：
- 核心链路完整
- 开发风险低
- 复试重点突出

## 21.2 小幅拔高版
在稳妥版基础上增加：
- 备份包
- 压缩
- 基础 AES
- 完整性验证

不要继续增加：
- 云备份
- SFTP
- 分布式
- 快照
- 复杂密钥管理
- 断点续传
- 备份轮转
- 防篡改链
- 复杂任务依赖

---

# 22. 三个流程与代码模块对应关系
| 业务流程 | 主要模块 |
|----------|----------|
| 数据备份 | BackupManager、FileScanner、FileComparator、Manifest、ArchiveWriter |
| 数据还原 | RestoreManager、ArchiveReader、Manifest、FileSystem |
| 自定义备份 | FileScanner、FileFilter、BackupManager |

辅助模块：
| 功能 | 模块 |
|------|------|
| 文件操作 | FileSystem |
| 日志 | Logger |
| 调度 | TaskScheduler |
| 压缩 | ICompressor |
| 加密 | IEncryptor |
| 校验 | HashCalculator |
| 配置 | BackupConfig |
| GUI | GUI Layer |

---

# 23. 用例图
## 23.1 参与者
用户

```text
                    ┌──────────────────┐
                    │   数据备份系统    │
                    │                  │
用户 ──────────────→│ 数据备份          │
 │                  │ 数据还原          │
 │                  │ 自定义备份        │
 │                  │ 备份任务管理      │
 │                  │ 查看备份历史      │
 │                  │ 查看日志          │
 │                  └──────────────────┘
```

## 23.3 Mermaid 用例关系图

---

# 24. 面试 / 复试重点
## 24.1 项目核心问题
面试官可能问：**为什么要做增量备份？**

回答：
全量备份每次都会处理全部文件。当数据量增加以后，重复复制未变化文件会产生较大的磁盘 I/O 和备份时间开销，因此增加增量备份，只处理发生变化的文件。

## 24.2 如何判断文件变化？
回答：
系统首先比较文件路径、大小和修改时间等元数据。对于可能发生变化的文件，再计算 Hash 进行进一步确认，从而减少不必要的文件内容读取。

## 24.3 为什么使用 Manifest？
回答：
Manifest 保存上一次备份对应的文件状态，包括路径、大小、修改时间和 Hash。下一次备份时可以将当前目录状态与历史状态进行比较，因此 Manifest 同时服务于增量检测、恢复和完整性验证。

## 24.4 为什么采用分层架构？
回答：
GUI、业务流程和文件系统操作的变化原因不同。将它们分开可以降低耦合，使文件备份核心可以独立测试，也方便后续替换 GUI 或增加压缩、加密等功能。

## 24.5 为什么不把所有代码写在 BackupManager？
回答：
BackupManager 负责组织备份流程，文件扫描、文件比较、文件操作、日志和压缩等具有独立职责。如果全部放在一个类中，会导致类职责过重，也会增加测试和后续修改的成本。

## 24.6 如果备份过程中一个文件突然被删除怎么办？
回答：
单个文件失败不会直接导致整个任务无提示退出。系统记录失败文件和具体错误，并根据任务策略继续处理其他文件，最终在 BackupResult 中统计成功和失败数量。

## 24.7 为什么 GUI 不直接操作文件？
回答：
GUI 只负责交互和展示。如果 GUI 直接承担文件扫描和备份逻辑，会导致界面代码和业务代码高度耦合。将核心业务独立出来后，可以直接对 BackupManager、FileComparator 等模块进行测试。

---

# 25. 简历项目描述
推荐写法：

> 基于 C++ / Windows 的数据备份系统
> 采用分层架构和面向对象设计，实现本地文件全量与增量备份、数据恢复、任务调度、备份历史及异常日志；设计 Manifest 保存文件路径、大小、修改时间和 Hash，通过"元数据快速判断 + Hash 二次确认"识别文件变更，减少重复文件处理；实现备份归档、压缩、基础 AES 加密及恢复后的完整性校验，并针对权限异常、文件消失、磁盘空间不足、备份取消等场景进行测试。

---

# 26. 项目演示流程
**演示一：新建备份任务**
```text
打开软件
    ↓
选择源目录
    ↓
选择备份目录
    ↓
选择全量备份
    ↓
开始
    ↓
显示进度
    ↓
备份完成
    ↓
查看历史
```

**演示二：增量备份**
第一次：
```text
100 个文件
    ↓
备份 100 个
```

修改：
```text
修改 3 个
新增 2 个
```

第二次：
```text
扫描 105 个
    ↓
Manifest 比较
    ↓
发现 5 个变化
    ↓
只备份 5 个
```

这是整个项目最值得演示的功能。

**演示三：恢复**
```text
选择备份
    ↓
选择恢复目录
    ↓
开始恢复
    ↓
创建目录
    ↓
恢复文件
    ↓
Hash 校验
    ↓
恢复完成
```

---

# 27. 项目代码审查标准
后续所有 C++ 代码按照以下标准审查。

**P0：严重问题**
必须修改：
- 编译错误
- 数据损坏风险
- 文件句柄泄漏
- 内存泄漏
- 未处理关键错误
- 核心业务使用脚本语言
- 核心业务依赖不符合课程要求的第三方工具
- 明确违反课程限制
- GUI 直接承担大量核心业务

**P1：结构问题**
优先修改：
- 巨型类
- 超长函数
- 高耦合
- 重复代码
- 全局状态过多
- 业务逻辑与文件操作严重混合
- 异常处理散乱
- 资源生命周期不清晰

**P2：代码质量**
建议修改：
- 命名问题
- 魔法数字
- 魔法字符串
- 无意义注释
- 头文件依赖混乱
- 不必要的复制
- 不合理的继承

---

# 28. 明确禁止的"屎山"结构
禁止：
```cpp
class BackupManager {
public:
    void backup()
    {
        // 扫描文件
        // 读取配置
        // 筛选文件
        // 排序
        // 计算 Hash
        // 复制文件
        // 打包
        // 压缩
        // 加密
        // 写日志
        // 更新 GUI
        // 处理异常
        // ...
    }
};
```

也禁止：
```text
MainWindow
    ↓
BackupManager
    ↓
BackupManager
    ↓
BackupManager
    ↓
FileSystem
    ↓
GUI
```

形成循环依赖。

---

# 29. 代码质量目标
最终代码应达到：
```text
                可运行
                   │
        ┌──────────┼──────────┐
        ↓          ↓          ↓
      正确       健壮       可维护
        │          │          │
        └──────────┼──────────┘
                   ↓
                可测试
                   ↓
                可扩展
                   ↓
               可解释 / 可答辩
```

项目不追求代码量。
项目追求：
少而清晰的模块 + 明确的数据流 + 可靠的文件操作 + 清楚的异常处理 + 可重复的测试

---

# 30. 最终技术路线
```text
                    用户
                      │
                      ↓
                     GUI
                      │
                      ↓
               Application Layer
                      │
           ┌──────────┴──────────┐
           ↓                     ↓
     TaskScheduler          BackupManager
                                  │
               ┌─────────────────┼─────────────────┐
               ↓                 ↓                 ↓
          FileScanner      FileComparator       FileFilter
               │                 │                 │
               └─────────────────┼─────────────────┘
                                 ↓
                              Manifest
                                 │
                                 ↓
                          ArchiveWriter
                                 │
                     ┌───────────┴───────────┐
                     ↓                       ↓
                Compressor              Encryptor
                     │                       │
                     └───────────┬───────────┘
                                 ↓
                            Backup File
                                 │
                                 ↓
                          ArchiveReader
                                 │
                     ┌───────────┴───────────┐
                     ↓                       ↓
               Decompressor              Decryptor
                     │                       │
                     └───────────┬───────────┘
                                 ↓
                          RestoreManager
                                 │
                                 ↓
                            FileSystem
                                 │
                                 ↓
                              Windows
```

---

# 31. 最终落地原则
**第一优先级**
- 全量备份
- 增量备份
- 恢复

**第二优先级**
- Manifest
- Hash
- 异常处理
- 日志
- 任务调度
- 测试

**第三优先级**
- 自定义筛选
- 备份历史
- GUI

**第四优先级**
- 打包
- 压缩
- AES
- 高级元数据
- 特殊文件

开发过程中，如果时间不足，按照优先级向下裁剪。
绝不能为了完成更多功能牺牲核心代码结构。

---

# 32. 项目最终验收标准
项目完成时至少应能够稳定完成：

**场景 A：全量备份**
```text
源目录
    ↓
100 个文件
    ↓
完整备份
```

结果：
- 100 个文件均存在
- 目录结构正确
- Manifest 正确

**场景 B：增量备份**
```text
第一次：100 个文件
第二次：修改 3 个 + 新增 2 个
```

结果：
- 只处理 5 个变化文件

**场景 C：恢复**
```text
备份数据
    ↓
新目录
    ↓
完整恢复
```

结果：
- 路径一致
- 文件数量一致
- 文件大小一致
- Hash 一致

**场景 D：异常**
至少成功验证：
- 源路径不存在
- 文件读取失败
- 目标路径无权限
- 备份文件损坏
- 错误密码
- 用户取消

**场景 E：工程结构**
代码应满足：
```text
GUI
    ↓
业务层
    ↓
文件引擎
    ↓
Windows
```

不存在核心业务全部集中在一个类或一个 GUI 回调函数中的情况。

---

# 33. 项目最终定位
本项目的核心竞争力不在于功能数量，而在于：

Windows 文件系统
    + 面向对象
    + 分层架构
    + Manifest
    + 增量备份
    + Hash
    + 任务调度
    + 异常容错
    + 测试

压缩和 AES 作为工程扩展能力。

项目最终应呈现为一个结构清晰、能够实际运行、具有完整软件工程过程的小型系统软件项目，而不是功能堆叠的课程作业。
